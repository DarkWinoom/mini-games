# pnpm 11+ configuration
# allowBuilds: explicitly allow (true) / disallow (false) install scripts
# esbuild: Vite 依赖，需要 postinstall 下载平台 binary
# vue-demi: pinia 依赖，需要 postinstall
allowBuilds:
  esbuild: true
  vue-demi: true
